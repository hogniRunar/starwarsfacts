A small webpage to display information about Star Wars things. It is written in VueJS to uses the http://swapi.co API

To install run the following commands:
git clone git@github.com:hogniRunar/starwarsfacts.git
npm install
